name: RDP Ultra Potato (Fixed - 8Bit No Anim)

on:
  workflow_dispatch:

jobs:
  rdp-extreme-lite:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v2

    - name: 🚀 Install Bore (Tunneling)
      run: |
        wget -q https://github.com/ekzhang/bore/releases/download/v0.5.0/bore-v0.5.0-x86_64-unknown-linux-musl.tar.gz
        tar -xf bore-v0.5.0-x86_64-unknown-linux-musl.tar.gz
        sudo mv bore /usr/local/bin/
        rm bore-v0.5.0-x86_64-unknown-linux-musl.tar.gz

    - name: 🧹 Hapus Bloatware
      run: |
        sudo apt-get remove -y azure-cli google-cloud-cli dotnet-sdk-* llvm-* mono-* temurin-* mysql* php* firefox || true
        sudo apt-get autoremove -y
        sudo apt-get clean
        sudo systemctl disable snapd.service || true
        sudo systemctl stop snapd.service || true

    - name: 📦 Install IceWM (Super Ringan)
      run: |
        sudo apt update
        # Install IceWM + PCManFM (biar desktop icon muncul)
        sudo DEBIAN_FRONTEND=noninteractive apt install -y icewm pcmanfm xterm chromium-browser tightvncserver x11-xserver-utils --no-install-recommends

    - name: ⚡ Kernel & Network Tweak
      run: |
        sudo sysctl -w net.ipv4.tcp_window_scaling=1
        sudo sysctl -w net.core.rmem_max=8388608
        sudo sysctl -w net.core.wmem_max=8388608
        echo "performance" | sudo tee /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor || true

    - name: 🔧 Config VNC & Desktop (Mode Burik)
      run: |
        mkdir -p ~/.vnc
        mkdir -p ~/.icewm
        
        echo "123456" | vncpasswd -f > ~/.vnc/passwd
        chmod 600 ~/.vnc/passwd

        # Config IceWM - Wireframe move (garis doang pas geser window)
        echo "Theme=\"default\"" > ~/.icewm/theme
        echo "OpaqueMove=0" >> ~/.icewm/preferences
        echo "OpaqueResize=0" >> ~/.icewm/preferences
        echo "ShowTaskBar=1" >> ~/.icewm/preferences

        # Script startup
        cat > ~/.vnc/xstartup << 'EOF'
        #!/bin/sh
        unset SESSION_MANAGER
        unset DBUS_SESSION_BUS_ADDRESS
        export XDG_SESSION_TYPE=x11
        
        xset s off
        xset -dpms
        xsetroot -solid black
        
        # Jalanin PCManFM sebagai desktop manager biar icon muncul
        pcmanfm --desktop &
        
        # Jalanin IceWM
        exec icewm-session
        EOF
        
        chmod +x ~/.vnc/xstartup

    - name: 🌐 Browser Anti-Lag (Chromium Tweak)
      run: |
        # FIX: Buat folder Desktop DULUAN sebelum bikin file!
        mkdir -p ~/Desktop
        
        # Bikin shortcut
        cat > ~/Desktop/Internet.desktop << 'EOF'
        [Desktop Entry]
        Version=1.0
        Type=Application
        Name=Chromium Fast
        Exec=chromium-browser --no-sandbox --disable-smooth-scrolling --disable-gpu --disable-software-rasterizer --disable-gpu-compositing --disable-animations --disable-gpu-vsync
        Icon=chromium-browser
        EOF
        
        chmod +x ~/Desktop/Internet.desktop

    - name: 🔥 Start VNC (8-Bit Color) & Bore
      run: |
        # Start VNC Server 8-bit color depth
        vncserver :1 -geometry 1280x720 -depth 8 -pixelformat bgr233
        
        echo "--------------------------------------------------------"
        echo "✅ VNC 8-BIT MODE SIAP!"
        echo "🔑 Password: 123456"
        echo "--------------------------------------------------------"

        # Jalanin Bore
        bore local 5901 --to bore.pub

name: macos-arm-vnc 

on: workflow_dispatch

jobs:
  run:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: 1. Setup User & Environment
        run: |
          # Matikan Sleep & Screen Saver
          sudo systemsetup -setcomputersleep Never
          sudo systemsetup -setdisplaysleep Never
          
          # Buat User Admin Baru
          sudo sysadminctl -addUser admin -fullName "Administrator" -password "password123" -admin

      - name: 2. Setup VNC Server
        run: |
          KICKSTART="/System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart"
          
          # Reset & Konfigurasi ulang
          sudo $KICKSTART -deactivate -configure -access -off
          sudo $KICKSTART -configure -allowAccessFor -allUsers -privs -all
          sudo $KICKSTART -configure -clientopts -setvnclegacy -vnclegacy yes
          
          # Set Password VNC: 12345678
          sudo $KICKSTART -configure -clientopts -setvncpw -vncpw 12345678
          
          # Restart Service
          sudo $KICKSTART -activate -restart -agent -console

      - name: 📸 AMBIL SCREENSHOT (DEBUG)
        run: |
          echo "Sedang mengambil screenshot layar server..."
          # Ambil screenshot layar utama
          screencapture -x /tmp/screen_debug.jpg
          
          echo "Mengupload screenshot..."
          # Upload ke 0x0.st (Hosting file sementara & cepat)
          curl -F "file=@/tmp/screen_debug.jpg" https://0x0.st
          
          echo " "
          echo "⬆️ KLIK LINK DI ATAS UNTUK MELIHAT TAMPILAN LAYAR SAAT INI ⬆️"

      - name: 3. Start Session
        run: |
          curl -sL https://github.com/ekzhang/bore/releases/download/v0.6.0/bore-v0.6.0-aarch64-apple-darwin.tar.gz | tar -xz
          sudo mv bore /usr/local/bin/
          
          echo "=========================================="
          echo "1. Password VNC: 12345678"
          echo "2. Jika layar hitam -> Klik tengah layar -> Ketik: password123 -> Enter"
          echo "=========================================="
          
          bore local 5900 --to bore.pub > bore.log 2>&1 &
          sleep 5
          cat bore.log
          
      - name: 4. Keep Alive 
        run: |
         end=$((SECONDS+21600))
         while [ $SECONDS -lt $end ]; do
            echo "Mac Alive - Sisa waktu: $(( (end - SECONDS) / 60 )) menit"
            sleep 60
         done
